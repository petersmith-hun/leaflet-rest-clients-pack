spring:
  config:
    activate:
      on-profile: hystrix-enabled

bridge.hystrix.enabled: true

---

spring:
  config:
    activate:
      on-profile: hystrix-disabled

bridge.hystrix.enabled: false

---

spring:
  config:
    activate:
      on-profile: hystrix-broken

bridge.hystrix.enabled: false

---

server.port: 9998
